// GENERATED by packages/tokens/generate.mjs from tokens.json (Kinetic Drop v2.0).
// DO NOT EDIT — change tokens.json and re-run the generator.

import SwiftUI

/// Motion tokens. Every animation must respect Reduce Motion at the call site.
public enum Motion {
    public static let fast: TimeInterval = 0.15
    public static let standard: TimeInterval = 0.3
    public static let pulseGlow: TimeInterval = 3
    public static let countdown: TimeInterval = 1
}

/// cubic-bezier easings from the design system, as iOS 16-compatible Animations.
/// Usage: .animation(Easing.standard(duration: Motion.standard), value: state)
public enum Easing {
    public static func standard(duration: TimeInterval) -> Animation {
        .timingCurve(0.4, 0, 0.2, 1, duration: duration)
    }
    public static func decelerate(duration: TimeInterval) -> Animation {
        .timingCurve(0, 0, 0.2, 1, duration: duration)
    }
    public static func accelerate(duration: TimeInterval) -> Animation {
        .timingCurve(0.4, 0, 1, 1, duration: duration)
    }
    public static func spring(duration: TimeInterval) -> Animation {
        .timingCurve(0.34, 1.56, 0.64, 1, duration: duration)
    }
}
